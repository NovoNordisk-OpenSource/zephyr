% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checkmate.R
\name{report_checkmate_assertations}
\alias{report_checkmate_assertations}
\title{Report collection of assertations}
\usage{
report_checkmate_assertations(
  collection,
  msg = "Invalid input(s):",
  env = parent.frame()
)
}
\arguments{
\item{collection}{A collection of assertations created with \code{\link[checkmate:AssertCollection]{checkmate::makeAssertCollection()}}}

\item{msg}{\code{\link[=character]{character()}} Header of the error message if any assertations failed}

\item{env}{\code{\link[=environment]{environment()}} Environment to use for the error message}
}
\description{
Improved reporting of \code{AssertCollections} created with the \code{\link[checkmate:AssertCollection]{checkmate::makeAssertCollection()}} package
using \code{\link[cli:cli_abort]{cli::cli_abort()}} instead of \code{\link[checkmate:AssertCollection]{checkmate::reportAssertions()}} in order to provide a more
informative error message.

The function is intended to be used inside a function that performs assertations on its input arguments.
See below for an example.
}
\examples{
add_numbers <- function(a, b) {
  collection <- checkmate::makeAssertCollection()
  checkmate::assert_numeric(x = a, add = collection)
  checkmate::assert_numeric(x = b, add = collection)
  report_checkmate_assertations(collection)
  return(a + b)
}

add_numbers(1, 2)
try(add_numbers(1, "b"))
try(add_numbers("a", "b"))

}
